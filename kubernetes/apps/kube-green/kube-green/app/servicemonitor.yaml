---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/servicemonitor_v1.json
# Replaces the chart's ServiceMonitor, which cannot send the bearer token that
# controller-runtime's TokenReview requires, so its scrape gets a 401. The name
# differs on purpose: a clash would let a Helm upgrade delete this one.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kube-green-metrics
spec:
  namespaceSelector:
    matchNames:
      - kube-green
  selector:
    matchLabels:
      app.kubernetes.io/name: kube-green
      control-plane: controller-manager
  endpoints:
    - port: https
      scheme: https
      interval: 30s
      path: /metrics
      bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
      tlsConfig:
        # The serving certificate is issued for the in-cluster Service name and
        # signed by an internal CA that Prometheus does not trust.
        insecureSkipVerify: true
