---
# yaml-language-server: $schema=https://json.schemastore.org/kubernetes
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netbox-media-rwx
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn-production-v1
  resources:
    requests:
      storage: 1Gi
