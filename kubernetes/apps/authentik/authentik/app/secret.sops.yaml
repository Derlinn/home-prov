---
# Générer les valeurs puis chiffrer :
#   secret-key     : openssl rand -base64 60 | tr -d '\n'
#   postgresql-password : openssl rand -base64 32 | tr -d '\n'
# Puis : sops --encrypt --in-place secret.sops.yaml
apiVersion: v1
kind: Secret
metadata:
  name: authentik-credentials
type: Opaque
stringData:
  secret-key: "CHANGEME_60_CHARS_RANDOM"
  postgresql-password: "CHANGEME_STRONG_PASSWORD"
