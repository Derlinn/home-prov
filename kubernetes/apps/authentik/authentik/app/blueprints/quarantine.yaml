# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
version: 1
metadata:
  name: quarantine
entries:
  - model: authentik_core.group
    id: quarantine-group
    state: present
    identifiers:
      name: linderis_quarantaine
    attrs:
      is_superuser: false
      users:
        - !Find [authentik_core.user, [username, theo]]
  - model: authentik_providers_oauth2.oauth2provider
    id: quarantine-provider
    state: present
    identifiers:
      name: Provider for Quarantine
    attrs:
      client_id: quarantaine
      client_secret: !Env AUTHENTIK_QUARANTAINE_CLIENT_SECRET
      client_type: confidential
      grant_types:
        - authorization_code
        - refresh_token
      sub_mode: hashed_user_id
      include_claims_in_id_token: true
      issuer_mode: per_provider
      authorization_flow: !Find
        - authentik_flows.flow
        - [slug, default-provider-authorization-explicit-consent]
      invalidation_flow: !Find
        - authentik_flows.flow
        - [slug, default-provider-invalidation-flow]
      signing_key: !Find
        - authentik_crypto.certificatekeypair
        - [name, authentik Self-signed Certificate]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
      redirect_uris:
        # /oauth2/callback is the Envoy Gateway SecurityPolicy.
        - matching_mode: strict
          redirect_uri_type: authorization
          url: https://quarantaine.${DOMAIN}/oauth2/callback
  - model: authentik_core.application
    id: quarantine-app
    state: present
    identifiers:
      slug: quarantaine
    attrs:
      name: Quarantaine
      provider: !KeyOf quarantine-provider
      policy_engine_mode: any
      icon: fa://fa-shield-halved
      group: Média
  - model: authentik_policies.policybinding
    state: present
    identifiers:
      target: !KeyOf quarantine-app
      group: !KeyOf quarantine-group
    attrs:
      order: 0
      enabled: true
