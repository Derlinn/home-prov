# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
version: 1
metadata:
  name: netbird
entries:
  - model: authentik_core.group
    id: netbird-group
    state: present
    identifiers:
      name: linderis_netbird
    attrs:
      is_superuser: false
  - model: authentik_providers_oauth2.oauth2provider
    id: netbird-provider
    state: present
    identifiers:
      name: Provider for NetBird
    attrs:
      client_id: netbird
      client_secret: !Env AUTHENTIK_NETBIRD_CLIENT_SECRET
      client_type: confidential
      grant_types:
        - authorization_code
        - refresh_token
      sub_mode: hashed_user_id
      include_claims_in_id_token: true
      issuer_mode: per_provider
      access_code_validity: minutes=1
      access_token_validity: minutes=5
      refresh_token_validity: days=30
      authorization_flow: !Find
        - authentik_flows.flow
        - [slug, default-provider-authorization-explicit-consent]
      invalidation_flow: !Find
        - authentik_flows.flow
        - [slug, default-provider-invalidation-flow]
      signing_key: !Find
        - authentik_crypto.certificatekeypair
        - [name, authentik Self-signed Certificate]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
      redirect_uris:
        # NetBird brokers through its own embedded Dex, so the callback is
        # Dex's, not the dashboard's. Confirm against Settings > Identity
        # Providers, which prints the exact URL it expects.
        - matching_mode: strict
          redirect_uri_type: authorization
          url: https://netbird.${DOMAIN}/oauth2/callback
        - matching_mode: strict
          redirect_uri_type: authorization
          url: https://netbird.${DOMAIN}/oauth2/logout/callback
  - model: authentik_core.application
    id: netbird-app
    state: present
    identifiers:
      slug: netbird
    attrs:
      name: NetBird
      provider: !KeyOf netbird-provider
      policy_engine_mode: any
      icon: https://cdn.jsdelivr.net/gh/homarr-labs/dashboard-icons/svg/netbird.svg
      group: Infrastructure
  - model: authentik_policies.policybinding
    state: present
    identifiers:
      target: !KeyOf netbird-app
      group: !KeyOf netbird-group
    attrs:
      order: 0
      enabled: true
