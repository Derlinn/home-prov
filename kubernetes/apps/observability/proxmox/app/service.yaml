---
apiVersion: v1
kind: Service
metadata:
  name: pve-exporter
  labels:
    app.kubernetes.io/name: pve-exporter
spec:
  selector:
    app.kubernetes.io/name: pve-exporter
  ports:
    - name: metrics
      port: 9221
      targetPort: metrics
