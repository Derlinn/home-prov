---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
#
# DEPRECATED (2026-08-11) : remplace par Gatus (../../gatus/).
# Conserve dans le repo pour historique, plus deploye par Flux.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrepository.yaml
  - ./secret.sops.yaml
  - ./helmrelease.yaml
  - ./servicemonitor.yaml
  - ./httproute.yaml
