---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Allow policies (evaluated first by Cilium)
  - ./host-fw-control-plane.yaml
  - ./host-fw-workers.yaml
